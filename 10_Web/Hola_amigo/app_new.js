
function app() {

    // Definir los nodos del DOM
    let domNombre = document.querySelector('#nombre') // me devuelve un objeto es decir un nodo de DOM

    // Definir manejadores de eventos
}

document.addEventListener('DOMContentLoaded', app)